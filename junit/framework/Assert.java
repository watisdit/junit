package junit.framework;

/**
 * A set of assert methods. Messages are only displayed when an assert fails.
 */

public class Assert extends junit.Assert {
	/**
	 * Protect constructor since it is a static only class
	 */
	protected Assert() {
	}
}
